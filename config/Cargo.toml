[package]
name = "config"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
directories = { workspace = true }
anyhow = { workspace = true }
tracing = { workspace = true }
# atexit registration for the persistence-writer drain — the desktop event
# loop exits via process::exit, so Rust-side Drop never runs.
libc = "0.2"
